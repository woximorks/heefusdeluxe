<%= form_with(model: project) do |form| %>
  <% if project.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
        <% project.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :project_title, style: "display: block" %>
    <%= form.text_field :project_title %>
  </div>

  <div>
    <%= form.label :project_url, style: "display: block" %>
    <%= form.text_field :project_url %>
  </div>

  <div>
    <%= form.label :project_description, style: "display: block" %>
    <%= form.text_area :project_description %>
  </div>

  <div>
    <%= form.label :project_image_url, style: "display: block" %>
    <%= form.text_field :project_image_url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
